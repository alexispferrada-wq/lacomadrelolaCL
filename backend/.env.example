# ── Server
PORT=3001
NODE_ENV=production

# ── Admin credentials (change these before deploying!)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=LaComadre2026!

# ── JWT
JWT_SECRET=change_this_to_a_random_secret_string_min_32_chars
JWT_EXPIRES_IN=8h

# ── CORS — allowed origins (comma-separated)
CORS_ORIGINS=https://alexispferrada-wq.github.io,http://localhost:3000,http://127.0.0.1:5500
